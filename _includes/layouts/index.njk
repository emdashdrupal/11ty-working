{% include 'layouts/partials/header.njk' %}

<section id="intro">
    <div class="float-right rounded-lg max-w-48">
        <img
        src="/assets/images/ed-marsh.jpg"
        alt="Ed Marsh, photo courtesy Arclight Images"
        title="Ed Marsh, photo courtesy Arclight Images"
      >
    </div>
    <div class="prose">
{{ content |safe }}

        {% include 'layouts/partials/goocal-button.html' %}</div>
</div>
</section>

<section class="border-2 mx-auto my-0 py-0" id="skills">
<h2 class="px-3">How I can help you</h2>
<p class="description px-3">Here are a few ways I can help your organization. <a href="/skills/">See all the ways I can help</a>.</p>
<div class="grid col-auto sm:grid-cols-4 gap-2 px-3">
    {% set collectionName = "skills" %}
    {% for item in collections[collectionName] %}
        {% if item.data.featured == true and item.data.featuredOrder > 0 | sort(collections[collectionName].data.featuredOrder) %}
            {% include 'layouts/partials/card.njk' %}
        {% endif %}
    {% endfor %}
</div>
</section>

<section class="border-2 mx-auto my-0 py-0" id="ssg">
<h2 class="px-3">Creating a static site</h2>
<p class="description px-3 prose">I converted my WordPress site to a static site with the <a href="https://www.11ty.dev/">Eleventy static site generator</a>. Here's what I learned about the process, and how these lessons learned can help you organize and structure your content to meet your goals.</p>
    <div class="col-span-full text-xl font-bold italic p-4">
        <a href="/static-site-transformation/">Check back frequently to read the full series!</a>
    </div>

<div class="grid col-auto sm:grid-cols-4 gap-2 px-2">
    {% set collectionName = "ssg" %}
    {% for item in collections[collectionName] %}
        {% if item.data.featured == true %}
            {% include 'layouts/partials/card.njk' %}
        {% endif %}
    {% endfor %}
</div>
</section>

<section class="border-2 mx-auto" id="podcasts">
<h2 class="px-3">Recent podcasts</h2>
<p class="description px-3 prose">I hosted (scheduled, recorded, edited, produced, published, promoted, and reviewed metrics for) the <a href="podcasts/">Content Content podcast</a>, where I interviewed "the people behind the content" in technical communication, content strategy, content marketing, implementation, and more.
    <span class="text-lg font-bold italic">
        <a href="/podcasts/">See all podcasts</a>
    </span>
</p>
<div class="grid col-auto sm:grid-cols-4 gap-2 px-3">
    {% set collectionName = "podcasts" %}
    {% for item in collections.podcasts | reverse %}
        {% if loop.index0 <= 3 %}
            {% include 'layouts/partials/card.njk' %}
        {% endif %}
    {% endfor %}

</div>
</section>

{% include 'layouts/partials/footer.njk' %}
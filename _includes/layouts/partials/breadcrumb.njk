  <nav aria-label="Breadcrumb">
    <ol class="breadcrumb">
      <li class="breadcrumb-item"><a href="/content">Home</a></li>
      {% for ancestor in page.data.navigation.ancestors %}
        <li class="breadcrumb-item"><a href="{{ url or page.url }}">{{ data.title or title }}</a></li>
      {% endfor %}
      <li class="breadcrumb-item active" aria-current="page"><a href="{{ url or page.url }}">{{ data.title or title }}</a></li>
    </ol>
{% from "layouts/partials/macros.njk" import dropdown,
listItem %}

    <div class="grid grid-cols-2 md:grid-cols-3 gap-2 py-0 px-2">
        <div id="tagline" class="col-span-1">
            <a href="/" class="font-sans text-whitish font-semibold">
                Ed Marsh creates successful technical content by building community
            </a>
        </div>

        <div id="menubar">
            <ul class="flex sm:flex-wrap md:flex-wrap mt-0 pt-0" aria-label="Main navigation menu" role="menubar">
                {{ dropdown('skillsDropdown', 'Skills', collections.skills, '/skills') }}
                {{ dropdown('ssgDropdown', 'Samples', collections.ssg, '/static-site-transformation/') }}
                {{ listItem('/podcasts/', 'Podcasts') }}
                {{ listItem('/about/about-ed-marsh', 'About') }}
                {{ listItem('/contact/', 'Contact') }}
            </ul>
        </div>
    </div>
{% from "layouts/partials/macros.njk" import dropdown, listItem %}

<nav class="container bg-green-blue mx-auto">
    <div class="space-y-4 px-4">
        <div class="text-xl">
            <span class="font-semibold text-whitish">Ed Marsh</span>
            <span class="font-normal text-lg text-whitish">&mdash; Putting the technical in technical communication since 1994</span>
        </div>
        <ul class="container font-sans flex flex-wrap text-whitish">
            {{ listItem('/', 'Home') }}
            {{ dropdown('ssgDropdown', 'Website transformation \>', collections.ssg, '/static-site-transformation/') }}
            {{ listItem('/podcasts/', 'Podcasts') }}
            {{ listItem('/technical-writing-examples/', 'Writing samples') }}
            {{ dropdown('skillsDropdown', 'Skills >', collections.skills, '/skills') }}
            {{ listItem('/about/about-ed-marsh', 'About') }}
            {{ listItem('/contact/', 'Contact') }}
        </ul>
    </div>
</nav>

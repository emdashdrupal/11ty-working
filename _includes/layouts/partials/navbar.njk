{% from "layouts/partials/macros.njk" import dropdown, listItem %}

<nav class="bg-medium-blue -mx-4 px-4 py-4 mb-6">
    <div class="flex flex-col space-y-4">
        <div class="text-xl">
            <span class="font-semibold text-whitish">Ed Marsh</span>
            <span class="text-whitish">&mdash;</span>
            <span class="font-normal text-lg text-whitish">technical writer, information architect, content strategist, podcaster</span>
        </div>
        <ul class="font-sans flex flex-wrap gap-4 text-whitish">
            {{ listItem('/', 'Home') }}
            {{ dropdown('ssgDropdown', 'Static site transformation', collections.ssg, '/static-site-transformation/') }}
            {{ listItem('/podcasts/', 'Podcasts') }}
            {{ listItem('/technical-writing-examples/', 'Writing samples') }}
            {{ dropdown('skillsDropdown', 'Skills', collections.skills, '/skills') }}
            {{ listItem('/about/about-ed-marsh', 'About') }}
        </ul>
    </div>
</nav>

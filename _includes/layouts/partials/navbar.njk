{% from "_includes/macros.njk" import dropdown, listItem %}

<nav class="bg-slate-400 shadow-md mb-6 container mx-auto px-4 flex justify-between">
    <div class="text-xl font-medium mt-1">Ed Marsh &mdash; technical writer, information architect, content strategist, podcaster</div>
    <ul class="flex space-x-4">
        {{ listItem('/content', 'Home') }}
        {{ dropdown('ssgDropdown', 'Static site transformation', collections.ssg) }}
        {{ listItem('/content/podcasts/', 'Podcasts') }}
        {{ listItem('/content/technical-writing-examples/', 'Writing Samples') }}
        {{ dropdown('skillsDropdown', 'Skills', collections.skills) }}
        {{ listItem('/content/about/about-ed-marsh', 'About') }}
    </ul>
</nav>